before_install:
  - sudo apt-get update -y
  - sudo apt-get install -y valgrind

branches:
  only:
    - master

os:
  - linux

language: 
  - cpp
  - python

script:
  - cd test/
  - python testing.py